#include <limits.h>#include "matrix.h"int chainMatrixMultiplication(int *array, int size) {    int matrix[size+1][size+1];    for (int i = 1; i <= size; i++)        matrix[i][i] = 0; // main diagonal    for (int length = 1; length < size; length++) {        for (int i = 1; i <= size - length; i++) {            int j = length + i;//            matrix[i][j] = INT_MAX;            int q = matrix[i][i] + matrix[i+1][j] + array[i-1] * array[i] * array[j];            matrix[i][j] = q;            for (int k = i+1; k < j; k++) {                int q = matrix[i][k] + matrix[k+1][j] + array[i-1] * array[k] * array[j];                if (q < matrix[i][j]) matrix[i][j] = q;            }        }    }    return matrix[1][size];}